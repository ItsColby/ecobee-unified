resume_program:
  name: Resume thermostat schedule
  description: Clear the selected thermostat's hold through its explicitly mapped local HomeKit button.
  target:
    entity:
      integration: ecobee_unified
      domain: climate
